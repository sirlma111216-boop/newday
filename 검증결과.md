# 업무달력 검증 결과

## 최종 스타일 변경

`DESIGN-clay.md`의 크림색 배경, 검정 버튼, 포화된 분류 색상, 청록 카드, 둥근 모서리와 낮은 그림자를 전체 화면에 적용했습니다. 데이터 구조와 일정·알림 처리 로직은 스타일 변경 과정에서 수정하지 않았습니다.

실제 앱 내 브라우저에서 확인한 화면:

- PC 월간 달력: 분류별 기간 막대, 오늘 강조, 마감 표시와 월간/목록 전환
- PC 업무 상세: 청록색 공통 정보 카드, 링크·체크리스트와 일정 연결
- 일정 수정 창: 날짜·시간·업무 선택, 입력 요소, 저장·취소 버튼
- 백업 및 가져오기: 민트 내보내기 카드와 파일 선택 영역
- 알림 및 설정: 라벤더 안내 카드, 권한 거부 상태와 앱 알림 목록
- 390px 모바일: 목록, 전체 화면 상세, 닫기, 가로 넘침 없음. 상세 패널의 상단 좌표 0, 너비는 스크롤바를 제외한 페이지 너비와 같음

검사 후 브라우저 크기를 원래 PC 크기로 되돌렸습니다. 실제 휴대전화 기기에서는 검증하지 않았습니다.

## 빌드와 핵심 로직

`npm run build`: TypeScript 검사 및 Vite 프로덕션 빌드 통과.

`npm test`: 11개 테스트 통과. 서울 날짜, 윤년·주·월 경계, 종료일 포함 기간, 겹침 배치, 잘못된 날짜·시간, 결합 필터, 복수 알림, 놓친 알림과 중복 방지, 완료·삭제 처리, 백업 검증, 빈 데이터의 예시 재생성 방지를 검사합니다.

## 실제 브라우저 기능 검사

스타일 변경 전 기능 구현 단계에서 별도 Edge 테스트 프로필로 다음 흐름을 확인했습니다. 사용 중인 미리보기의 데이터는 이 자동 검증에 사용하지 않았습니다.

| 흐름 | 결과 |
|---|---|
| 예시 12개 최초 생성 | 통과 |
| 제목·날짜만으로 추가, 새로고침 유지 | 통과 |
| 날짜 수정·역전 범위 거부·주/월 경계 표시 | 통과 |
| 실제 마우스 드래그 이동·기간 끝 늘리기·저장 | 통과 |
| 공통 체크리스트·메모·원노트 원문 링크 공유 | 통과 |
| HTML 모양 메모를 코드 실행 없이 텍스트로 표시 | 통과 |
| 검색·분류·미완료 필터 결합·업무 완료 전파 | 통과 |
| JSON 다운로드·가져오기 건수 안내·확인 전 미교체 | 통과 |
| 확인 후 연결 관계 복원, 잘못된 JSON·관계 거부 | 통과 |
| 알림 설정 UI, 제어된 브라우저 시계에서 도착·새로고침 중복 방지 | 통과 |
| 페이지를 닫고 예정 시각 이후 재실행하여 놓친 알림 수집 | 통과 |
| 390px 목록·상세·모바일 편집 저장 | 통과 |
| 실행 중 브라우저 오류 | 수집된 오류 없음 |

추가 검사: ‘외 N개’ 전체 목록, 저장 공간 오류 시 입력 보존과 재시도, 빈 백업으로 교체 후 샘플 미생성 모두 통과했습니다.

## 확인 범위의 제한

- 알림 시간 경과는 브라우저 테스트 시계를 사용했습니다. 브라우저 종료·절전 상태의 실시간 예약 알림을 검증하거나 보장하지 않습니다.
- 브라우저 권한 허용·거부·미지원 및 테스트 알림 호출은 모의 Notification API로 검증했습니다. 실제 운영체제 알림 배너 표시는 확인하지 못했습니다. 최종 앱 내 브라우저에서는 권한 차단 상태에서도 알림 목록이 표시되는 것을 확인했습니다.
- 실제 원노트 앱 실행과 단락 이동은 확인하지 않았습니다. 테스트용 주소는 분리된 검증 프로필에만 사용했고, 기본 예시에는 가짜 원노트 주소를 포함하지 않습니다.
- 브라우저 프로세스 자체를 완전히 종료한 후 같은 영구 프로필로 재실행하는 검사는 수행하지 않았습니다. 실제 새로고침·페이지 재열기, localStorage 저장과 재로딩을 검증했습니다.
- 선택적인 WebMCP 일정 조회 도구는 등록된 상태까지 확인했습니다. 호출 검증은 자동 승인 검토가 사용량 제한을 이유로 거부하여 수행하지 못했습니다. 일반 사용자 UI 동작과는 별개입니다.

공개 배포는 하지 않았으며 `npm start`로 로컬 실행합니다.

## 2026-09-15 Firebase 연결 코드 추가

- Google 로그인, Firestore 실시간 구독, 서버 저장 응답 확인, 계정 분리, 버전 충돌 거부, 공통 알림 기록 트랜잭션을 구현했습니다.
- 기존 11개 검사와 클라우드 직렬화·버전 충돌·빈 데이터·잘못된 데이터·UTF-8 크기 제한 5개 검사를 합쳐 16개 자동 검사가 통과했습니다.
- 실제 Firebase 프로젝트 설정이 아직 없어 Google 로그인, Firestore 서버 왕복·규칙 및 두 실제 기기의 동기화는 미확인입니다. Firebase Emulator 검사도 실행하지 않았습니다.
- GitHub 전송 프로그램 git-remote-https.exe가 Windows에서 충돌하여 원격 전송을 중단했습니다. 로컬 소스 저장과 빌드는 이 문제와 별개입니다.

- Firebase 코드 추가 후 인앱 브라우저에서 일정 추가·새로고침 유지·메모 수정·삭제를 확인했습니다. 날짜 입력 이벤트에서 화면값과 저장값이 달라지는 문제를 수정했고, 9월 15~17일 기간이 종료일 포함으로 저장됨을 재확인했습니다.
- 390px 너비의 목록·공통 업무 상세와 가로 넘침 여부를 확인했습니다. 해당 검증 중 브라우저 오류 로그는 없었습니다. 임시 검증 일정은 삭제했습니다.
- TypeScript 및 Vite 프로덕션 빌드를 통과했습니다. Firebase SDK 추가로 단일 JS 파일 크기 경고가 있으며, 실행을 막는 오류는 아닙니다.
