# 업무달력 검증 결과

## 최종 스타일 변경

`DESIGN-clay.md`의 크림색 배경, 검정 버튼, 포화된 분류 색상, 청록 카드, 둥근 모서리와 낮은 그림자를 전체 화면에 적용했습니다. 데이터 구조와 일정·알림 처리 로직은 스타일 변경 과정에서 수정하지 않았습니다.

실제 앱 내 브라우저에서 확인한 화면:

- PC 월간 달력: 분류별 기간 막대, 오늘 강조, 마감 표시와 월간/목록 전환
- PC 업무 상세: 청록색 공통 정보 카드, 링크·체크리스트와 일정 연결
- 일정 수정 창: 날짜·시간·업무 선택, 입력 요소, 저장·취소 버튼
- 백업 및 가져오기: 민트 내보내기 카드와 파일 선택 영역
- 알림 및 설정: 라벤더 안내 카드, 권한 거부 상태와 앱 알림 목록
- 390px 모바일: 목록, 전체 화면 상세, 닫기, 가로 넘침 없음. 상세 패널의 상단 좌표 0, 너비는 스크롤바를 제외한 페이지 너비와 같음

검사 후 브라우저 크기를 원래 PC 크기로 되돌렸습니다. 실제 휴대전화 기기에서는 검증하지 않았습니다.

## 빌드와 핵심 로직

`npm run build`: TypeScript 검사 및 Vite 프로덕션 빌드 통과.

`npm test`: 11개 테스트 통과. 서울 날짜, 윤년·주·월 경계, 종료일 포함 기간, 겹침 배치, 잘못된 날짜·시간, 결합 필터, 복수 알림, 놓친 알림과 중복 방지, 완료·삭제 처리, 백업 검증, 빈 데이터의 예시 재생성 방지를 검사합니다.

## 실제 브라우저 기능 검사

스타일 변경 전 기능 구현 단계에서 별도 Edge 테스트 프로필로 다음 흐름을 확인했습니다. 사용 중인 미리보기의 데이터는 이 자동 검증에 사용하지 않았습니다.

| 흐름 | 결과 |
|---|---|
| 예시 12개 최초 생성 | 통과 |
| 제목·날짜만으로 추가, 새로고침 유지 | 통과 |
| 날짜 수정·역전 범위 거부·주/월 경계 표시 | 통과 |
| 실제 마우스 드래그 이동·기간 끝 늘리기·저장 | 통과 |
| 공통 체크리스트·메모·원노트 원문 링크 공유 | 통과 |
| HTML 모양 메모를 코드 실행 없이 텍스트로 표시 | 통과 |
| 검색·분류·미완료 필터 결합·업무 완료 전파 | 통과 |
| JSON 다운로드·가져오기 건수 안내·확인 전 미교체 | 통과 |
| 확인 후 연결 관계 복원, 잘못된 JSON·관계 거부 | 통과 |
| 알림 설정 UI, 제어된 브라우저 시계에서 도착·새로고침 중복 방지 | 통과 |
| 페이지를 닫고 예정 시각 이후 재실행하여 놓친 알림 수집 | 통과 |
| 390px 목록·상세·모바일 편집 저장 | 통과 |
| 실행 중 브라우저 오류 | 수집된 오류 없음 |

추가 검사: ‘외 N개’ 전체 목록, 저장 공간 오류 시 입력 보존과 재시도, 빈 백업으로 교체 후 샘플 미생성 모두 통과했습니다.

## 확인 범위의 제한

- 알림 시간 경과는 브라우저 테스트 시계를 사용했습니다. 브라우저 종료·절전 상태의 실시간 예약 알림을 검증하거나 보장하지 않습니다.
- 브라우저 권한 허용·거부·미지원 및 테스트 알림 호출은 모의 Notification API로 검증했습니다. 실제 운영체제 알림 배너 표시는 확인하지 못했습니다. 최종 앱 내 브라우저에서는 권한 차단 상태에서도 알림 목록이 표시되는 것을 확인했습니다.
- 실제 원노트 앱 실행과 단락 이동은 확인하지 않았습니다. 테스트용 주소는 분리된 검증 프로필에만 사용했고, 기본 예시에는 가짜 원노트 주소를 포함하지 않습니다.
- 브라우저 프로세스 자체를 완전히 종료한 후 같은 영구 프로필로 재실행하는 검사는 수행하지 않았습니다. 실제 새로고침·페이지 재열기, localStorage 저장과 재로딩을 검증했습니다.
- 선택적인 WebMCP 일정 조회 도구는 등록된 상태까지 확인했습니다. 호출 검증은 자동 승인 검토가 사용량 제한을 이유로 거부하여 수행하지 못했습니다. 일반 사용자 UI 동작과는 별개입니다.

공개 배포는 하지 않았으며 `npm start`로 로컬 실행합니다.

## 2026-09-15 Firebase 연결 코드 추가

- Google 로그인, Firestore 실시간 구독, 서버 저장 응답 확인, 계정 분리, 버전 충돌 거부, 공통 알림 기록 트랜잭션을 구현했습니다.
- 기존 11개 검사와 클라우드 직렬화·버전 충돌·빈 데이터·잘못된 데이터·UTF-8 크기 제한 5개 검사를 합쳐 16개 자동 검사가 통과했습니다.
- 실제 Firebase 프로젝트 설정이 아직 없어 Google 로그인, Firestore 서버 왕복·규칙 및 두 실제 기기의 동기화는 미확인입니다. Firebase Emulator 검사도 실행하지 않았습니다.
- GitHub 전송 프로그램 git-remote-https.exe가 Windows에서 충돌하여 원격 전송을 중단했습니다. 로컬 소스 저장과 빌드는 이 문제와 별개입니다.

- Firebase 코드 추가 후 인앱 브라우저에서 일정 추가·새로고침 유지·메모 수정·삭제를 확인했습니다. 날짜 입력 이벤트에서 화면값과 저장값이 달라지는 문제를 수정했고, 9월 15~17일 기간이 종료일 포함으로 저장됨을 재확인했습니다.
- 390px 너비의 목록·공통 업무 상세와 가로 넘침 여부를 확인했습니다. 해당 검증 중 브라우저 오류 로그는 없었습니다. 임시 검증 일정은 삭제했습니다.
- TypeScript 및 Vite 프로덕션 빌드를 통과했습니다. Firebase SDK 추가로 단일 JS 파일 크기 경고가 있으며, 실행을 막는 오류는 아닙니다.

## 2026-09-21 달력 직접 입력
- 날짜 빈칸 클릭 → 회색 미지정 점과 입력란 표시. Enter 또는 저장으로 등록, Escape 또는 취소로 취소.
- 날짜 사이 드래그 → 양 끝 날짜를 포함하는 회색 선과 제목 입력란. 역방향 선택과 주 경계 지원.
- 저장된 일정 클릭 → 제목·시작일·종료일 및 분류 색상을 편집하는 창.
- 미지정 분류를 백업 검증·필터·저장 모델에 추가. 기존 네 분류 데이터는 그대로 읽음.
- 브라우저 확인: 한국어 직접 입력, 저장, 분류 변경, 기간 저장(9/10~9/15), 새로고침 유지, 역방향 선택(9/15→9/9), 기존 일정 드래그 이동(9/8→9/9), 휴대전화 390px 목록 가로 넘침 없음.
- 자동 검사 17개 및 빌드 통과. 기존 번들 크기 경고는 남아 있음.
- 이번 확인은 별도 브라우저 저장 미리보기(127.0.0.1:5174)에서 수행. 운영 Firebase 데이터 변경 및 GitHub/Cloudflare 재배포는 수행하지 않음.
- 실제 한글 IME 조합 키 입력, 이번 변경 후 기간 끝 핸들 드래그 및 실제 휴대전화 터치 드래그는 별도 검증하지 않음.

## 2026-09-22 일정 상세·변경 통일 및 삭제 개선
- 달력·목록에서 일정을 누르면 모달 대신 오른쪽 ‘일정 상세 및 변경’ 패널이 열립니다. 여기서 일정명·시작일·종료일·분류를 바로 고치고 ‘세부 내용 더 보기’로 시간·관련 업무·메모·자료·알림까지 편집합니다.
- 패널 하단에 ‘삭제’(빨간색, 글자 표시)와 ‘변경 저장’ 버튼을 나란히 배치했습니다. 삭제는 기존 확인창을 거칩니다.
- 새 일정 추가는 그대로 모달로 열립니다. 기존 읽기 전용 상세 패널(DetailPanel)은 제거했습니다.
- 브라우저 확인(로컬 저장 모드): 일정 클릭 → 편집 패널 표시, 제목 수정 후 저장 반영, 연속 2회 저장 시 ‘다른 기기에서 변경’ 오탐 없음, 삭제 확인 후 일정 제거(11→10개)와 패널 닫힘, 새 일정은 모달로 열림, 휴대전화 390px에서 가로 넘침 없음. 콘솔 오류 없음.
- 자동 검사 17개 및 빌드 통과.
- 이번 확인은 로컬 저장 미리보기에서 수행했으며, 운영 Firebase 데이터 변경·재배포는 하지 않았습니다.

## 2026-09-23 원노트 단락 링크 저장 오류 수정
- 원인: 주소 검사(safeLink)가 제어문자 범위를 `\u0000-\u0020`으로 잡아 **공백(\u0020)까지 거부**했습니다. 원노트 단락 링크에는 전자 필기장·구역·페이지 이름의 공백이 그대로 들어오므로(예: `내 전자 필기장`, `새 구역`) 정상 링크가 매번 거부됐습니다.
- 수정: 실제 제어문자(`\u0000-\u001f`, `\u007f`)만 거부하고 가운데 공백은 허용합니다. 앞뒤 공백은 브라우저가 지워 `' javascript:'`가 실행될 수 있으므로 계속 막습니다.
- 붙여 넣을 때 딸려 온 앞뒤 공백은 저장 시 자동으로 다듬어 더 이상 저장을 막지 않습니다.
- 오류 문구를 분리했습니다. 이전에는 이름 누락과 주소 오류가 같은 문장이었는데, 이제 ‘자료 링크 2의 표시 이름을 입력해 주세요.’ / ‘자료 링크 2 ‘안내 문서’의 주소를 확인해 주세요…’처럼 몇 번째 링크의 무엇이 문제인지 알려 줍니다.
- 브라우저 확인(로컬 저장 모드): 공백 포함 원노트 단락 링크 저장 성공, 새로고침 후 공백까지 원문 그대로 보존, 이름 누락·http 주소에서 각각 새 문구 표시, 콘솔 오류 없음.
- 자동 검사 17개(공백 포함 원노트 링크 회귀 검사 추가) 및 빌드 통과. `http:`·`data:`·`javascript:`·앞뒤 공백·줄바꿈·탭 주소는 계속 차단됨을 14개 사례로 확인했습니다.

## 2026-09-23 관련 자료를 분류 아래로 올리고 원노트 아이콘 적용
- 관련 자료(링크) 항목을 ‘세부 내용 더 보기’ 안에서 꺼내 분류(수업·학교업무·…) 칸 바로 아래로 옮겼습니다. 일정을 열면 접지 않아도 링크가 바로 보이고 바로 추가할 수 있습니다.
- 접기 항목 설명에서 ‘자료’를 빼고 ‘시간 · 관련 업무 · 메모 · 알림’으로 고쳤습니다.
- 바로가기 버튼을 원노트 보라색(#7719AA) 아이콘으로 바꿨습니다. onenote: 주소뿐 아니라 학교 SharePoint·OneDrive 주소(예: khgms-my.sharepoint.com)도 원노트 링크로 인식합니다. 그 밖의 일반 웹 주소는 기존 화살표 아이콘을 그대로 씁니다.
- 바로가기 아이콘이 `<a>`라 세로 정렬이 어긋나던 문제를 CSS로 맞췄습니다(휴지통 아이콘과 같은 줄, 가운데 정렬).
- 브라우저 확인: 일정 클릭 시 접기 없이 관련 자료 표시, 원노트 링크는 보라색 N 아이콘·일반 웹 주소는 화살표 아이콘, 링크 추가·삭제 동작, 휴대전화 390px과 데스크톱 1400px 모두 가로 넘침 없음, 콘솔 오류 없음.
- 자동 검사 17개 및 빌드 통과.

## 2026-09-23 일반 링크 지구 아이콘, 시간 박스 접기 밖으로 이동
- 일반 웹 주소의 바로가기 아이콘을 화살표에서 지구본으로 바꿨습니다. 원노트·SharePoint·OneDrive 주소는 계속 보라색 원노트 아이콘을 씁니다.
- 시간 조정 박스(종일 일정 체크·시작/종료 시간·마감일로 표시)를 ‘세부 내용 더 보기’ 밖으로 꺼내 관련 자료 바로 아래에 두었습니다.
- 접기 항목 설명을 실제 남은 내용에 맞춰 ‘관련 업무 · 메모 · 준비할 일 · 알림’으로 고쳤습니다.
- 패널 순서: 일정명 → 시작일·종료일 → 분류 → 관련 자료 → 시간 조정 → 세부 내용 더 보기 → 삭제·변경 저장.
- 브라우저 확인: 원노트 링크는 보라색 N, 일반 웹 주소(korea.kr)는 지구본 아이콘 표시, 종일 해제 시 시작/종료 시간 입력 등장, 변경 버리기로 원래 데이터 유지, 휴대전화 390px·데스크톱 1400px 모두 정상, 콘솔 오류 없음.
- 자동 검사 17개 및 빌드 통과.

## 2026-09-23 설정 화면 추가 (개인정보 · 휴일 · 분류)
- 사이드바 아래 ‘설정’을 추가하고 개인정보 / 휴일 / 분류 세 개 탭으로 나눴습니다. 앞으로 설정이 늘어나면 이 화면에 탭을 더 붙이면 됩니다.
- 개인정보: 이름, 근무 학교, 부서·담당, 메모를 저장합니다. 계정 데이터에 함께 저장되며 백업 파일에도 포함됩니다.
- 공휴일 자동 연동: 공공데이터포털(한국천문연구원 특일 정보) 인증키를 넣으면 그 자료를 쓰고, 비워 두면 무인증 공개 자료를 씁니다. 확인 결과 apis.data.go.kr 은 브라우저에서 바로 호출되어(CORS 허용) 별도 프록시 서버가 필요 없었습니다. 연도별로 받아 30일간 캐시하며, 해가 바뀌면 그 해 자료를 자동으로 받습니다. ‘연결 확인’ 버튼으로 즉시 검증할 수 있습니다.
- 무인증 자료에는 제헌절(2008년 공휴일 제외)과 근로자의 날(학교 정상 운영)이 섞여 있어 코드에서 걸러 냅니다. 2026년 기준 16일이 남는 것을 확인했습니다.
- 달력 표시: 토·일요일과 공휴일은 날짜를 빨간색으로, 공휴일은 이름(예: 추석)을 함께 표시합니다. 학교 지정 휴일은 옅은 배경과 명칭으로 표시합니다.
- 학교 지정 휴일: 명칭과 시작일·종료일(하루 또는 기간)을 등록합니다. 재량휴업일·방학 등에 사용합니다.
- 분류 설정: 분류의 이름과 색(12색 팔레트)을 바꾸고, 추가·삭제할 수 있습니다. 이름을 바꾸면 그 분류를 쓰던 업무와 사이드바 필터 선택도 함께 따라옵니다. 분류를 지우면 그 업무는 남은 분류로 옮깁니다. 고정 CSS 클래스(cat-0~4) 대신 설정 색에서 --cat-* 를 직접 만들어 씁니다.
- 호환성: 설정이 없던 예전 백업도 기본 설정으로 복원되고, 설정에 없는 분류를 쓰는 업무가 있으면 그 분류를 되살려 데이터를 잃지 않습니다.
- 브라우저 확인(로컬 저장 모드): 설정 열기·세 탭 이동, 개인정보 저장, 연결 확인(2026년 16일), 학교 휴일 3일 기간 등록 후 달력에 명칭·배경 표시, 분류 이름 변경(학교업무→행정업무)과 색 변경이 달력·목록·필터·편집 패널에 모두 반영, 업무 2건이 새 이름으로 이동(옛 이름 0건), 휴대전화 390px 가로 넘침 없음, 콘솔 오류 없음.
- 자동 검사 19개(설정 기본값·이전 백업 호환·분류 검증·학교 휴일 기간 조회 추가) 및 빌드 통과.

## 2026-09-23 검색을 ‘찾아서 이동’ 방식으로 변경하고 검색창 테두리 수정
- 검색창 안쪽에 테두리가 한 겹 더 생기던 문제를 고쳤습니다. clay.css의 `input:focus-visible` 초점 테두리가 검색 알약 안쪽에 그려지면서, 바깥 칸의 `.search:focus-within` 테두리와 겹쳐 두 겹으로 보였습니다. 이제 초점 테두리는 바깥 칸에만 줍니다.
- 검색이 달력을 걸러 내지 않습니다. 예전에는 검색하면 맞는 일정만 남고 나머지가 사라졌는데, 이제 달력·목록은 그대로 두고 검색창 아래에 결과 목록이 뜹니다.
- 결과를 누르면 그 일정의 달로 이동하고 오른쪽 상세·변경 패널이 열립니다. 분류 필터에 가려진 일정이면 그 분류를 다시 켜서 달력에도 보이게 합니다.
- 키보드: ↓·↑로 이동(끝에서 순환), Enter로 열기, Esc로 닫기. 마우스로 올려도 선택이 따라옵니다.
- 다가오는 일정을 먼저, 지난 일정은 최근 것부터 보여 주고 최대 12개까지 표시하며 그보다 많으면 총 개수를 알려 줍니다.
- 검색 범위는 이전과 같습니다. 일정명·업무명·메모·준비할 일·자료 링크 이름.
- 검색이 더는 거르지 않으므로 달력 부제목의 ‘○○ 검색 결과’ 문구를 원래대로 되돌렸습니다.
- 브라우저 확인: 검색어 입력 시 달력 일정 수가 그대로(10개→10개), 결과 클릭으로 9월→10월 이동과 패널 열림, 키보드 ↓↓↑ 이동과 Esc 닫힘, 초점 시 input outline none·바깥 칸에만 테두리, 휴대전화 390px 가로 넘침 없음, 콘솔 오류 없음.
- 한글 IME로 조합 중 누른 Enter는 글자 확정에 쓰이고 이동은 한 번 더 눌러야 합니다. 브라우저 표준 동작입니다.
- 자동 검사 20개(검색 정렬·제한·비필터 확인 추가) 및 빌드 통과.

## 2026-09-23 공공데이터포털 인증키 처리 보강
- 인증키는 저장소나 빌드 파일에 넣지 않습니다. 설정 화면에 입력해 사용자 데이터(브라우저 저장 또는 Firebase 계정)에만 저장합니다. 공개 저장소에 키가 올라가지 않는 것을 확인했습니다.
- 특일 정보 API 문서가 판에 따라 `serviceKey` 와 `ServiceKey` 를 섞어 써서, 두 가지를 차례로 시도하도록 했습니다.
- 오류 원인을 한국어로 안내합니다. 미등록 키(발급 직후 반영 대기), 활용 신청 미승인, 일일 호출 한도 초과, 사용 기간 만료를 각각 구분해 알려 줍니다. 공공데이터포털이 200 응답에 XML 오류를 돌려주는 경우도 처리합니다.
- End Point 는 화면에 표시된 `https://apis.data.go.kr/B090041/openapi/service/SpcdeInfoService` 와 일치합니다.
- 자동 검사 20개 및 빌드 통과.

## 2026-09-23 달력 표시 설정 추가, 알림 설정을 설정 탭으로 이동
- 설정에 ‘달력’ 탭을 추가했습니다. 주 시작 요일(일/월), 칸 높이(좁게·보통·넓게), 글자 크기(작게·보통·크게), 글꼴(기본·시스템·맑은 고딕·나눔고딕), 한 칸에 표시할 일정 수(2~6개), 토요일 색(파란색·빨간색), 기본 보기(자동·월간·목록)를 고를 수 있습니다.
- 알림 설정을 별도 창에서 설정 안 ‘알림’ 탭으로 옮겼습니다. 사이드바의 ‘알림 설정’과 상단 종 아이콘 모두 설정의 알림 탭을 바로 엽니다. 탭이 다섯 개라 좁은 화면에서는 가로로 넘겨 보며, 고른 탭은 자동으로 화면 안으로 끌어옵니다.
- 주 시작 요일을 바꿀 수 있게 되면서 요일 색을 칸 위치(:first-child/:last-child)가 아니라 실제 요일로 정하도록 고쳤습니다. 월요일 시작에서도 토·일 색이 제자리에 찍힙니다.
- 토요일 색을 고를 수 있게 되어, 일요일과 공휴일만 항상 빨간색으로 두고 토요일은 설정을 따릅니다. 기본값은 한국 달력 관례대로 파란색입니다.
- 칸 높이·글자 크기·글꼴은 앱 최상단에 CSS 변수로 내려보내 달력·목록에 함께 적용합니다. 글꼴은 body 가 .app 의 조상이라 변수만으로는 바뀌지 않아 font-family 를 직접 지정했습니다.
- 설정 값을 연달아 바꿀 때 앞의 변경이 덮어써지던 문제를 고쳤습니다. 이전 상태에서 이어받도록(함수형 갱신) 바꿨습니다. 실제로 7개를 한 번에 바꿔 모두 저장되는 것을 확인했습니다.
- 표시 설정은 취향일 뿐이라 값이 이상하면 가져오기를 막지 않고 기본값으로 되돌립니다. 설정이 없던 예전 백업도 기본값으로 복원됩니다.
- 브라우저 확인: 7개 설정 모두 저장·적용(월요일 시작, 칸 210px, 글자 1.16배, 맑은 고딕, 한 칸 5개, 토요일 빨강), 요일 머리글이 월~일로 바뀌고 토·일 색이 따라감, 종 아이콘이 알림 탭을 염, 휴대전화 390px에서 탭 가로 스크롤과 선택 탭 자동 노출, 가로 넘침 없음, 콘솔 오류 없음.
- 자동 검사 22개(주 시작 요일 격자·요일 머리글·표시 설정 복원 추가) 및 빌드 통과.

## 2026-09-23 분류 이름을 바꾸면 업무가 엉뚱한 분류로 옮겨가던 문제 수정
- 증상: 분류 이름을 바꾸고 저장해도 달력 색이 바뀌지 않거나, 엉뚱한 색으로 바뀌었습니다.
- 원인: 설정 화면이 ‘원래 이름 → 바뀐 이름’ 표를 **이름으로** 찾아 만들고 있었습니다. ‘수업 → 학교업무’처럼 이미 있는 이름으로 바꾸면 두 분류가 잠깐 같은 이름을 공유해, 다음 개명이 엉뚱한 분류를 가리켰습니다.
- 재현: 기본 분류를 수업→학교업무, 학교업무→외부강의, 연수→경희대, 개인→Spirit, 미지정→공모전으로 한 칸씩 밀어 개명했더니, ‘수업’ 업무가 학교업무가 아니라 외부강의로, ‘학교업무’ 업무는 그대로 학교업무로 남아 서로 뒤바뀌었습니다.
- 수정: 개명표를 저장할 때 분류 **id** 를 맞대어 만듭니다. 이름은 중간에 겹칠 수 있지만 id 는 변하지 않습니다. 이름 변경·색 변경·삭제 모두 id 기준으로 처리하고, 상태 갱신도 이전 상태에서 이어받도록 바꿔 연달아 고칠 때 앞의 변경이 덮어써지지 않게 했습니다.
- 재확인: 같은 연쇄 개명에서 수업→학교업무, 학교업무→외부강의, 연수→경희대, 개인→Spirit 로 업무가 정확히 따라왔습니다. 일정의 분류를 바꾸고 ‘변경 저장’ 했을 때도 달력 색이 즉시 바뀝니다(#ffb084 → #a4d4c5 확인).
- 자동 검사 23개(연쇄 개명·이름 맞교환·분류 삭제 회귀 검사 추가) 및 빌드 통과. 콘솔 오류 없음.
- 주의: 이번 수정은 앞으로의 저장을 바로잡습니다. 이미 잘못 옮겨진 기존 일정의 분류는 자동으로 되돌릴 수 없으므로, 일정을 눌러 분류를 다시 지정해 주셔야 합니다.

## 2026-09-23 휴대전화 화면 가독성 개선
- 확인한 문제(390×844 기준)
  - 월간 달력이 `min-width: 650px` 로 고정되어 있어 **한 주 7일 중 4일만 보이고 가로로 밀어야** 나머지가 보였습니다.
  - 상단이 3줄(브랜드 / 검색 / 일정 추가)로 쌓여 달력 시작 전에 **화면의 41%(350px)** 를 썼습니다.
  - 좁은 칸에 일정 제목을 그대로 넣어 글자가 뭉개지고, 공휴일 이름(추석)이 일정 막대에 가려 잘렸습니다.
- 수정
  - 한 주가 화면에 다 들어오도록 가로 스크롤을 없앴습니다. 칸 높이도 줄여 한 화면에 달 전체가 보입니다.
  - 상단을 2줄로 줄였습니다. 원인은 검색창을 `.search-box` 로 감싸면서 기존 `order: 9` 규칙이 더 이상 먹지 않아 일정 추가 버튼이 아래로 밀린 것이었고, 감싼 칸에 다시 지정했습니다. 달 제목과 월간/목록 전환도 한 줄로 합쳤습니다.
  - 좁은 칸에서는 제목 대신 **분류 색 막대**만 보여 주고, 날짜를 누르면 그날 일정 목록이 열립니다. 목록에서 일정을 눌러 상세·변경으로 들어가고 ‘이 날짜에 일정 추가’로 추가합니다. 휴대전화에서는 끌어서 기간 만들기를 끄고 안내 문구도 감췄습니다.
  - 공휴일 이름이 가려지지 않도록 일정 막대 시작 위치를 내렸습니다.
- 함께 고친 것: ‘외 N개’ 표시가 아직 3줄로 굳어 있어, 설정에서 한 칸에 표시할 일정 수를 3개가 아닌 값으로 바꾸면 잘못 세고 겹쳤습니다. 설정값을 따르도록 고쳤습니다.
- 결과: 달력 시작 위치 350px → 220px, 상단 173px → 113px, 한 주 7일 모두 표시, 가로 스크롤 없음.
- 확인: 휴대전화 390px에서 월간·목록·상세 패널·날짜별 목록 모두 가로 넘침 없이 읽힘. 태블릿 768px과 데스크톱 1400px은 기존 그대로(일정 제목 표시, 칸 높이 156px, 안내 문구 유지). 콘솔 오류 없음.
- 자동 검사 23개 및 빌드 통과.

## 2026-09-23 분류 전체 선택 추가, 관련 자료를 주소만 입력하도록 변경
- 사이드바 분류 위에 ‘전체’ 체크 줄을 넣었습니다. 누르면 전체 선택·해제가 되고, 일부만 켜져 있으면 중간 상태(indeterminate)로 표시합니다. 옆에 전체 일정 수를 함께 보여 줍니다.
- 관련 자료에서 ‘표시 이름’ 칸을 없애고 주소 한 칸만 남겼습니다. 열기 버튼(원노트·지구본)과 삭제 버튼은 그대로입니다.
- 표시 이름을 더 이상 받지 않으므로 백업 검사에서도 이름을 요구하지 않습니다. 예전에 입력해 둔 이름은 지우지 않고 데이터에 그대로 보존합니다. 주소가 잘못된 링크는 이름과 무관하게 계속 거부합니다.
- 오류 문구도 주소만 안내하도록 바꿨습니다.
- 브라우저 확인: 전체 체크 → 해제 시 일정 0개, 다시 선택 시 13개 복귀, 하나만 해제하면 중간 상태와 8개 표시. 이름 없이 주소만 넣은 링크가 오류 없이 저장되고 기존 이름은 그대로 남았습니다. 휴대전화 390px에서 링크 줄이 한 줄로 줄어 읽기 편해졌고 가로 넘침 없음. 콘솔 오류 없음.
- 자동 검사 24개(이름 없는 링크 저장·기존 이름 보존·잘못된 주소 거부 추가) 및 빌드 통과.

## 2026-09-23 추가 기능 모드와 할 일 목록 추가
- 설정에 ‘추가 기능’ 탭을 만들었습니다. 기본은 모두 꺼진 상태이고, 켜면 위쪽 ‘업무달력’ 옆에 아이콘이 생깁니다. 앞으로 만들 기능도 이 탭에 항목만 추가하면 같은 방식으로 붙습니다.
- 첫 기능으로 ‘할 일 목록’을 넣었습니다. 탭 두 개(해야 할 일 · 완료한 일)로 나뉜 메모입니다.
  - 동그라미를 누르면 밑줄이 그어지고 흐려지면서 ‘완료한 일’로 넘어갑니다. 다시 누르면 해야 할 일로 되돌아옵니다.
  - 해야 할 일은 끌어서 순서를 바꿉니다. 휴대전화는 끌기가 안 되므로 위·아래 화살표 버튼도 함께 두었고, 좁은 화면에서는 끌기 손잡이를 감춥니다.
  - 완료한 일은 최근에 끝낸 것이 맨 위, 가장 먼저 끝낸 것이 맨 아래로 쌓이며 오른쪽에 완료 일시를 표시합니다.
  - 아이콘에 남은 할 일 개수를 배지로 표시합니다.
- 할 일은 일정과 같은 저장소에 담겨 기기 간 동기화와 백업에 함께 포함됩니다. 할 일이 없던 예전 백업은 빈 목록으로 복원되고 추가 기능은 꺼진 상태가 됩니다.
- 브라우저 확인: 기본 상태에서 아이콘 없음 → 설정에서 켜면 아이콘과 배지 표시, 3개 추가, 화살표로 순서 변경, 완료 시 밑줄·흐림·완료 탭 이동·완료 일시 표시, 두 번째 완료 항목이 위로 쌓임(가장 먼저 한 일이 아래), 휴대전화 390px에서 상단 113px 유지·가로 넘침 없음, 콘솔 오류 없음.
- 자동 검사 26개(순서 변경·완료 정렬·되돌리기·백업 검증 추가) 및 빌드 통과.

## 2026-09-23 일정 공유(읽기 전용) 추가
- 설정에 ‘공유’ 탭을 만들었습니다. 공유할 분류를 고르고 ‘공유 번호 만들기’를 누르면 24자리 번호가 생깁니다. 번호 복사, 지금 내용으로 갱신, 공유 중지를 할 수 있습니다.
- 공유 방식: 고른 분류에 속한 업무와 그 일정만 **따로 복사해** `shares/{번호}` 문서에 저장합니다. 브라우저에서 걸러 보여 주는 방식은 개발자 도구로 전체가 드러나 보안이 되지 않기 때문입니다.
- 공유본에 담기지 않는 것: 고르지 않은 분류, 개인정보(이름·학교·부서·메모), 공휴일 인증키, 알림, 할 일, 일정별 알림 설정. 학교 지정 휴일은 달력 표시에 필요해 함께 담습니다.
- 볼 수 있는 사람: **로그인한 사람 중 번호를 아는 사람만**입니다(firestore.rules 의 `shares` 규칙). 쓰기는 만든 본인만 가능하므로 보는 사람은 고치거나 지울 수 없습니다.
- 보는 화면: 위에 ‘…을 보는 중입니다 · 읽기 전용’ 띠와 ‘나가기’ 버튼이 고정됩니다. 보는 동안 내 달력은 그려지지 않으며, 나가기를 눌러야 돌아옵니다(보던 번호는 브라우저에 남아 새로고침해도 유지됩니다).
- 달력에 `readOnly` 모드를 넣어 공유 화면에서는 끌어서 만들기·옮기기·종료일 조절이 모두 꺼지고, 날짜를 누르면 그날 목록만 열립니다.
- 자기 달력을 아직 못 여는 사람도 로그인 후 게이트 화면에서 공유 번호를 넣어 들어올 수 있습니다.
- 브라우저 확인: 브라우저 저장 모드에서는 공유 탭이 ‘Firebase 동기화가 필요하다’고 안내함. 공유 번호를 넣은 상태로 열면 읽기 전용 띠와 나가기 버튼이 뜨고, 권한이 없으면 한국어 안내가 나오며, 나가기를 누르면 저장된 번호가 지워지고 내 달력으로 돌아옵니다. 콘솔 오류 없음.
- 자동 검사 28개(공유 사본이 고른 분류만 담고 개인정보·인증키·알림·할 일을 제외하는지, 번호 형식·중복 없음 확인 추가) 및 빌드 통과.
- 남은 일: 실제 공유 왕복(번호 생성 → 다른 계정에서 보기)은 Firebase 프로젝트에 새 `shares` 보안 규칙을 배포한 뒤에야 동작합니다. 규칙 배포는 계정 소유자만 할 수 있어 이번 확인에서는 제외했습니다.

## 2026-09-23 보안 규칙 잠김 해제, 사이드바 정리, D-day 대비 개선
- **접근 권한 오류 원인**: 제가 드린 `firestore.rules` 에 프로젝트 초기 설정용 자리표시자 `'REPLACE_WITH_YOUR_UID'` 가 그대로 남아 있었습니다. 그 파일을 게시하면서 기존에 본인 UID 가 들어 있던 규칙을 덮어썼고, `request.auth.uid == 'REPLACE_WITH_YOUR_UID'` 는 참이 될 수 없어 본인도 막혔습니다. 배포 전에 이 자리표시자를 알려 드리지 않은 제 잘못입니다.
- 수정: UID 를 직접 적는 방식을 없애고 `request.auth.uid == userId` 만 쓰는 표준 규칙으로 바꿨습니다. 각자 자기 경로만 읽고 쓰므로 남의 데이터는 볼 수 없고, 바꿔 넣어야 할 자리표시자가 없어 같은 실수가 다시 나지 않습니다. 공유받는 사람도 로그인할 수 있어야 하므로 이 방식이 맞습니다.
- 사이드바에서 ‘내 캘린더’ 글자와 ‘업무달력’ 버튼을 지우고 접기 아이콘만 남겼습니다.
- D-day 배지를 분홍 바탕·검은 글씨에서 진한 빨강(#b1332a)·흰 글씨로 바꿨습니다. 흰색 대비 약 6.2:1 로 작은 글씨에서도 잘 읽힙니다. 달력 안 배지와 목록 배지 모두 적용했습니다.
- 공유 번호 입력칸을 로그인 게이트에서 빼고 설정 > 공유 탭에만 남겼습니다.
- 확인: 사이드바 상단이 접기 아이콘만 남고 바로 ‘분류’로 이어짐, 달력·목록의 D-day 가 흰 글씨/진한 빨강, 콘솔 오류 없음. 자동 검사 28개 및 빌드 통과.
